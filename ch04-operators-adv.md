---
theme: penguin
class: text-center
highlighter: shiki
lineNumbers: true
drawings:
  persist: false

fonts:
  provider: none
title: 程式基本運算（進階／自學）
routeAlias: ch04adv
style: |
  .slidev-layout p,
  .slidev-layout li,
  .slidev-layout td,
  .slidev-layout th,
  .slidev-layout div {
    font-size: max(16px, 1em);
  }
  table {
    width: 100%;
    margin: 1rem 0;
    border-collapse: collapse;
  }
  th, td {
    padding: 8px !important;
    border: 1px solid #e2e8f0 !important;
  }
  .index-table td {
    text-align: center;
    font-family: monospace;
  }
---

<div class="flex flex-col justify-center items-center h-full" style="background: #ffffff;">
  <p style="color: #5eada0; font-size: 1rem; font-weight: 600; letter-spacing: 0.2em; text-transform: uppercase; margin-bottom: 1.2rem;">Java Programming Masterclass</p>
  <h1 style="color: #1a5c5c; font-size: 3.4rem; font-weight: 900; line-height: 1.15; margin-bottom: 1.5rem;">程式基本運算</h1>
  <div style="height: 4px; width: 320px; background: linear-gradient(90deg, #5eada0, #a7d9d0); border-radius: 2px; margin-bottom: 1.5rem;"></div>
  <p style="color: #4a7c7c; font-size: 1.15rem; font-style: italic;">進階自學內容</p>
  <Link to="home" style="color:#9dc4c4;font-size:0.85rem;margin-top:2rem;text-decoration:none;letter-spacing:0.05em;">← 返回目錄</Link>
</div>

<!--
大家好，歡迎來到第 4 章的進階自學內容。基礎版我們已經學完加減乘除、比較、邏輯運算這些「日常用」的運算子，今天要繼續往下挖兩個比較硬的主題：位元運算子，還有完整的運算子優先順序表。

為什麼要學這個？這就像是學完了汽車的「開車」，現在要打開引擎蓋看看裡面長什麼樣子。位元運算不是寫一般商業程式的必要技能，但如果你以後想往底層、效能優化、加密演算法、或者面試想多一個亮點，這塊知識會很有幫助。

學完這份自學內容，我們會能直接用二進位的角度看數字，知道 &、|、^、~、<<、>>、>>> 這些符號到底在做什麼，也能看懂一份「優先順序表」並用它來解釋程式碼的執行順序。
-->

---
layout: default
---

# Outline

- **位元邏輯運算子**（&、|、^、~）
- **位元移位運算子**（<<、>>、>>>）
- **完整運算子優先順序表**（15 個層級）
- **進階練習：位元操作 + 優先順序綜合應用**

<!--
這份自學內容分成兩條主線：第一條是「位元運算」，我們會學會怎麼用 &、|、^、~ 在二進位的層級操作數字，還有用 <<、>>、>>> 做位移；第二條是「優先順序表」，把基礎版學過的運算子全部按優先順序排好，再加上一些基礎版沒提到的運算子，例如 instanceof 跟三元運算子 ?:。

生活化比喻的話，可以把位元運算想成是在「拆解樂高積木最小的那一顆顆粒」，而優先順序表則像是一張「誰先誰後」的排隊規則表，平時用不到，但遇到複雜運算式時就是救命索。
-->

---
layout: section
class: flex flex-col justify-center items-center text-center
---

# 第一部分
# 位元運算子

<!--
進入位元運算之前，先想像一個情境：電腦裡的每一個數字，骨子裡都是一串 0 與 1。我們平常寫的 +、-、*、/ 是站在「十進位數字」的角度操作，而位元運算子則是直接站在「0 與 1」的角度操作。

這部分我們會認識四個位元邏輯運算子（&、|、^、~），以及三個位移運算子（<<、>>、>>>）。聽起來很硬，但其實規則並不多，掌握規則後就會發現它們其實蠻有規律的。
-->

---
layout: default
---

# 位元邏輯運算子的語法結構

| 運算子 | 名稱 | 說明 |
| --- | --- | --- |
| `&` | 位元 AND | 兩個位元皆為 1 才得 1 |
| `\|` | 位元 OR | 任一位元為 1 即得 1 |
| `^` | 位元 XOR | 兩個位元不同才得 1 |
| `~` | 位元 NOT | 所有位元取反（~N = -(N+1)） |

<div class="mt-4 p-3 bg-blue-50 border-l-4 border-blue-400 text-gray-700 text-sm text-left">
💡 <b>逐位元運算：</b> 這四個運算子會把兩個整數的每一個位元分別拿出來比較，運算完成後再組合回一個新的整數。
</div>

<!--
我們可以把 & 想成是「兩個人都要舉手才算數」——左邊的位元跟右邊的位元都是 1，結果才是 1，其中一個是 0 就直接判輸。

| 想成「只要有一個人舉手就算數」，比 & 寬鬆很多。^（XOR）則是最有趣的，它代表「立場不同才算數」——兩個位元一樣（都是 0 或都是 1）結果就是 0，不一樣才是 1。最後 ~ 是「全部反過來」，原本的 0 全部變 1，1 全部變 0。

業界實務上，這幾個運算子常出現在權限旗標（flag）管理、圖層遮罩（mask）、或是加密演算法裡，面試時也常被拿來考二進位的理解程度。
-->

---

# 位元邏輯運算子 — 範例

```java
int a = 5;  // 二進位：0101
int b = 7;  // 二進位：0111
System.out.println(a & b);  // 5  (0101)
System.out.println(a | b);  // 7  (0111)
System.out.println(a ^ b);  // 2  (0010)
System.out.println(~a);     // -6 (位元全反)
```

<!--
這段範例用 a=5（0101）跟 b=7（0111）做示範，我們可以先把這兩個數字的二進位寫在紙上，再一行一行對照結果。

帶讀的關鍵在 a & b 跟 a ^ b 這兩行：& 是「兩邊都 1 才留 1」，所以 0101 跟 0111 比對後只有第 1、第 3 位都是 1，結果是 0101 也就是 5；^ 是「不同才留 1」，兩者只有最後一位不同（1 跟 1 其實相同，但第 2 位 0 跟 1 不同），所以結果是 0010 也就是 2。

⚠️ 易錯點：~a 的結果是 -6，不是「把 5 變成它的負數」這麼直覺。因為 Java 用「補數」表示負數，~a 的規則是 ~N = -(N+1)，這點第一次看到通常都會愣一下，記住這個公式就好，不用真的去算二進位補數。

預期結果：依序印出 5、7、2、-6。
-->

---
layout: default
---

# 位元移位運算子的語法結構

| 運算子 | 名稱 | 說明 | 等效 |
| --- | --- | --- | --- |
| `<<` | 左移 | 位元向左移，右補 0 | `n * 2^位移量` |
| `>>` | 帶符號右移 | 位元向右移，左補符號位 | `n / 2^位移量` |
| `>>>` | 無符號右移 | 位元向右移，左補 0（正負數皆補 0）| 只補 0 |

<div class="mt-4 p-3 bg-blue-50 border-l-4 border-blue-400 text-gray-700 text-sm text-left">
💡 <b>用途：</b> 位移運算比乘除法更快，常用於底層效能最佳化或旗標（flag）管理。
</div>

<!--
位移運算子可以想成是「把一整排數字往左或往右搬」。<< 往左搬，右邊空出來的位置補 0，效果等於乘以 2 的位移次方；>> 往右搬，效果等於除以 2 的位移次方，但要注意左邊補進來的是「符號位」，也就是正數補 0、負數補 1，這樣才能維持原來的正負號。

>>> 跟 >> 的差別只在「左邊一律補 0」，不管原來是正數還是負數。這代表如果對一個負數做 >>>，結果反而會變成一個很大的正數，這是它跟 >> 最大的不同。

業界實務上，位移運算因為直接操作硬體層級的位元，速度比乘除法快很多，常見於遊戲引擎、圖形處理、或是需要極致效能的底層程式。
-->

---

# 位元移位運算子 — 範例

```java
int n = 4;          // 二進位：00000100
System.out.println(n << 2);   // 16（左移 2 位，等於 4 * 4）
System.out.println(n >> 1);   // 2 （右移 1 位，等於 4 / 2）
System.out.println(-1 >>> 1); // 2147483647（最大正整數）
```

<!--
這段範例分三行，每一行都呈現一種位移運算子的效果。第一行 n << 2 把 4 往左移兩位，等於乘以 2 的 2 次方也就是 4，所以結果是 16。第二行 n >> 1 把 4 往右移一位，等於除以 2，結果是 2。

第三行是這份自學內容的重點：-1 >>> 1。-1 在二進位裡是「全部都是 1」（32 個 1），用 >>> 往右移一位，最左邊補 0，結果變成「0 加上 31 個 1」，換算成十進位就是 2147483647，也就是 int 能表示的最大正整數。

⚠️ 易錯點：很多人第一次看到 -1 >>> 1 會以為結果跟 -1 >> 1 一樣（都是 -1），但其實 >>> 因為「無條件補 0」，會把負數直接變成正數。這是 >> 跟 >>> 唯一但關鍵的差異。

預期結果：依序印出 16、2、2147483647。
-->

---
layout: default
---

# 練習 0：位元 — 解題邏輯
### 任務說明

寫一個程式，宣告兩個整數 `permission1 = 0b0110`（代表「讀+寫」權限）與 `permission2 = 0b0101`（代表「讀+執行」權限），完成下列計算並印出結果：

1. 使用 `&` 計算兩者「共同擁有」的權限（交集）
2. 使用 `|` 計算兩者「合併後」的權限（聯集）
3. 使用 `^` 計算兩者「只有一邊擁有」的權限（互斥或）
4. 將 `permission1` 使用 `<<` 左移 1 位，印出結果

**提示：** 二進位的 `0b0110` 等於十進位 `6`，`0b0101` 等於十進位 `5`。

<!--
【任務鋪陳】
這個練習把位元邏輯運算子（&、|、^）跟位移運算子（<<）放在同一個情境裡——「權限旗標」管理，這正是上一頁提到的業界實務應用場景。

【引導思考】
想一想：`&` 代表「兩邊都要有才算」，對應到權限管理上是什麼意思？`|` 代表「任一邊有就算」，又對應什麼？把 0b0110 跟 0b0101 各自的二進位寫出來，逐位元比對，答案就會浮現。
-->

---
layout: default
---

# 練習 0：位元 — 解題邏輯
### 解題提示

1. `permission1 & permission2`：逐位元比對，兩邊都是 1 才得 1 → 共同權限
2. `permission1 | permission2`：任一邊是 1 即得 1 → 合併權限
3. `permission1 ^ permission2`：兩邊不同才得 1 → 互斥權限
4. `permission1 << 1`：所有位元左移一位，右補 0 → 等於乘以 2

```java
int permission1 = 0b0110; // 6，讀+寫
int permission2 = 0b0101; // 5，讀+執行

System.out.println(permission1 & permission2); // 4 (0b0100，共同：讀)
System.out.println(permission1 | permission2); // 7 (0b0111，合併：讀+寫+執行)
System.out.println(permission1 ^ permission2); // 3 (0b0011，互斥：寫+執行)
System.out.println(permission1 << 1);          // 12 (0b1100)
```

<!--
【帶讀解法】
這個練習的核心是把 0110 跟 0101 兩組位元，逐位比對：第 3 位（從右數第 3 位）兩邊都是 1，所以 & 的結果在這一位是 1；其他位元至少有一邊是 0，所以 & 的結果是 0100，也就是 4。

| 的結果則是只要任一邊有 1 就算 1，所以是 0111（7）；^ 的結果是「不同才算 1」，所以是 0011（3）。

最後 permission1 << 1 把 0110 整體左移一位，變成 1100（12），驗證了「左移 n 位 = 乘以 2 的 n 次方」這個規則。完成這個練習，也代表我們對「位元運算可以用來管理權限旗標」這個業界應用有了具體的體會。
-->

---
layout: section
class: flex flex-col justify-center items-center text-center
---

# 第二部分
# 運算子優先順序表

<!--
基礎版我們已經接觸過運算子的優先順序，但只挑了最常用的那幾個說明。這份自學內容要把整張表攤開來看，從最高優先（括號）一路排到最低優先（指定運算子），總共 15 個層級。

想像這張表是一份「誰先誰後」的排隊規則：當一個運算式裡同時出現好幾種運算子，Java 不是從左到右隨便算，而是先看誰的「優先序」比較高，誰就先算。看懂這張表，遇到複雜的運算式時就能不靠猜測、直接推算出結果。
-->

---
layout: default
---

# 完整運算子優先順序表（高 → 低，第 1–8 層）

| 優先順序 | 運算子 | 結合方向 |
| --- | --- | --- |
| 1（最高） | `()` 括號、`[]` 陣列索引、`.` 成員存取 | 左至右 |
| 2 | `++` `--`（後置） | 左至右 |
| 3 | `+` `-`（一元）、`~` `!`、`++` `--`（前置） | 右至左 |
| 4 | `*` `/` `%` | 左至右 |
| 5 | `+` `-` | 左至右 |
| 6 | `<<` `>>` `>>>` | 左至右 |
| 7 | `<` `>` `<=` `>=` `instanceof` | 左至右 |
| 8 | `==` `!=` | 左至右 |

<!--
這一頁是前半張表，從最高優先的括號一路到比較運算子。重點是第 2、3 層：後置的 `x++` 其實比前置的 `++x` 優先序更高一階（JLS 上是兩種不同的語法產生式），但兩者都比乘除法還高，所以 -x * y 會先算 -x 再乘 y，這跟我們直覺想的「先乘除」並沒有衝突，只是一元運算子排得更前面。

第 6 層的位移運算子，優先順序比加減法低，但比比較運算子高，這代表像 a + b << 1 這種寫法，會先算 a + b 再位移，而不是先位移再加。這也是為什麼業界建議遇到位移運算時最好加括號，避免讓讀程式的人猜錯順序。

業界實務上，這張表平常不需要背，寫程式時遇到不確定的地方直接加括號就好；但看懂表格能幫助我們在 Code Review 時快速判斷別人寫的運算式到底會怎麼執行。
-->

---

# 完整運算子優先順序表（高 → 低，第 9–15 層）

| 優先順序 | 運算子 | 結合方向 |
| --- | --- | --- |
| 9 | `&`（位元 AND） | 左至右 |
| 10 | `^`（位元 XOR） | 左至右 |
| 11 | `\|`（位元 OR） | 左至右 |
| 12 | `&&`（邏輯 AND） | 左至右 |
| 13 | `\|\|`（邏輯 OR） | 左至右 |
| 14 | `?:`（三元運算子） | 右至左 |
| 15（最低） | `=` `+=` `-=` `*=` `/=` `%=`（指定類） | 右至左 |

```java
// 綜合示範：位元運算子與邏輯運算子的優先順序
int a = 5, b = 3;
boolean result = a > 0 & b > 0 || a == b;
// & 的優先順序（第 9 層）高於 ||（第 13 層）
// 先算 (a > 0) & (b > 0) → true & true → true
// 再算 true || (a == b) → true
System.out.println(result); // true
```

<!--
這一頁是後半張表，從位元運算子一路到指定運算子。注意第 9～11 層的位元邏輯運算子（&、^、|），優先順序居然比第 12～13 層的邏輯運算子（&&、||）還高！這代表 a & b || c 會先算 a & b，再跟 c 做 ||，這跟很多人「邏輯運算子優先」的直覺剛好相反。

範例這一行 boolean result = a > 0 & b > 0 || a == b; 故意把位元 AND（&）跟邏輯 OR（||）放在一起，目的是讓我們看到 & 真的會先被計算。因為 a > 0 跟 b > 0 都是 true，所以 true & true 先得到 true，再跟 a == b（false）做 ||，最終結果是 true。

⚠️ 易錯點：在條件判斷裡誤用 & 或 | 取代 && 或 ||，雖然語法上合法（因為兩邊都是 boolean），但會「失去短路求值」的效果，也可能因為優先順序不同而算出意料之外的結果。建議條件判斷一律使用 && 和 ||，& 和 | 留給真正的位元運算。

最低優先的永遠是指定類運算子（=、+=...），這點符合直覺：永遠是「先把右邊全部算完，最後才塞進左邊的變數」。
-->

---
layout: default
---

# 練習 1：運算子優先順序表
### 認證模擬題（單選）

觀察下面這段程式碼：

```java
int a = 8, b = 2;
boolean result = a > 5 | b > 5 && a < 10;
System.out.println(result);
```

請依照完整運算子優先順序表，判斷這段程式碼的輸出結果是什麼？

A. `false`，因為 `|` 的優先順序比 `&&` 高，會先算 `a > 5 | b > 5`
B. `true`，因為 `&&` 的優先順序比 `|` 高，會先算 `b > 5 && a < 10`
C. 編譯錯誤，因為 `boolean` 不能使用 `|` 運算子
D. `true`，因為從左到右依序計算，`a > 5` 為 `true` 時就直接短路，後面不會執行

<!--
【出題動機】
這題想測驗大家是不是真的記得「位元邏輯運算子（&、^、|）優先順序比邏輯運算子（&&、||）高」這個容易被直覺誤導的規則，並且能實際套用優先順序表推算結果。

【解題引導】
先查表確認 `|`（第 11 層）跟 `&&`（第 12 層）誰的優先順序比較高，再決定要先算哪一段。算出兩個比較運算式的布林值之後，再依照優先順序把它們組合起來。
-->

---
layout: default
---

# 練習 1：運算子優先順序表
### 解析

**正確答案：B**

- A. ❌ 順序剛好相反——`&&`（第 12 層）的優先順序比 `|`（第 11 層）**低**，所以不是先算 `|` 那一段
- B. ✅ `&&`（第 12 層）優先順序比 `|`（第 11 層）高，先算 `b > 5 && a < 10` → `false && true` → `false`；再算 `a > 5 | false` → `true | false` → `true`
- C. ❌ `|` 可以用在兩個 `boolean` 之間，作為「位元 OR」運算，語法合法，只是會失去短路求值
- D. ❌ `|` 不像 `||` 一樣有短路求值機制，左右兩側都會被執行；而且這題的優先順序也不是單純從左到右

<!--
【帶讀解法】
這題故意把 `|` 跟 `&&` 放在一起，目的就是要打破「邏輯運算子優先」的直覺。依照優先順序表，`&&`（第 12 層）其實比 `|`（第 11 層）先算，所以正確的拆解順序是：先算 `b > 5 && a < 10`（`2 > 5` 為 `false`，`&&` 短路後直接是 `false`），再算 `a > 5 | false`（`8 > 5` 為 `true`，`true | false = true`）。

⚠️ 易錯點：如果照「從左到右」的直覺去算，會先算 `a > 5 | b > 5`，得到 `true`，再跟 `a < 10` 做 `&&`，也會得到 `true`——這題剛好兩種算法答案相同，但這只是巧合！換成不同的數字，兩種算法很可能得到不同結果。這正是為什麼業界建議混用位元運算子跟邏輯運算子時，務必加上括號明確標示意圖。
-->

---
layout: section
class: flex flex-col justify-center items-center text-center
---

# 第三部分
# 進階練習

<!--
學完位元運算子跟完整的優先順序表，接下來用兩個練習來驗收成果。第一個練習回到位元運算的基本應用，第二個練習則會把位元運算跟優先順序表結合在一起，做一次綜合演練。
-->

---
layout: default
---

# 練習 2：位元操作三連問
### 任務說明

給定一個整數，使用位元運算子回答以下問題：

1. 這個整數是偶數還是奇數？（提示：用 `&` 運算子）
2. 這個整數乘以 4 的結果為何？（提示：用左移運算子）
3. 這個整數除以 2 的結果為何？（提示：用右移運算子）

**要求：** 使用 Scanner 讀取整數輸入，並分別用位元運算子完成以上三個計算。

<!--
回顧一下，我們剛剛學了 & 可以逐位元比對，<< 跟 >> 可以做位移。這個練習就是要把這兩種運算子實際用在數字上。

引導思考一下：一個整數的二進位最後一位，跟「奇數、偶數」之間有什麼關係？如果最後一位是 0，這個數字會是奇數還是偶數？想清楚這點，第一小題就解開了。
-->

---

# 練習 2：位元操作三連問
### 解題提示

1. **判斷奇偶：** `(n & 1) == 0` 表示偶數，`(n & 1) == 1` 表示奇數（最低位元為 0 代表偶數）
2. **乘以 4：** `n << 2`（左移 2 位 = 乘以 2² = 乘以 4）
3. **除以 2：** `n >> 1`（右移 1 位 = 除以 2，向下取整）

```java
int n = 12;
System.out.println((n & 1) == 0 ? "偶數" : "奇數"); // 偶數
System.out.println(n << 2);   // 48（12 * 4）
System.out.println(n >> 1);   // 6 （12 / 2）
```

<div class="mt-4 p-3 bg-blue-50 border-l-4 border-blue-400 text-gray-700 text-sm text-left">
💡 <b>效能：</b> 位元運算子執行速度比算術運算更快，是底層程式與效能最佳化的利器。
</div>

<!--
n & 1 為什麼能判斷奇偶？因為任何整數的二進位最後一位，只有在這個數是奇數時才會是 1（例如 12 是 1100，最後一位是 0，所以是偶數）。用 & 1 把其他位元全部「遮掉」，只留下最後一位，就能直接看出答案。

這個練習也順便驗證了左移、右移跟乘除法的對應關係：n << 2 真的等於 n * 4，n >> 1 真的等於 n / 2（向下取整）。感覺像是在直接跟電腦的「神經系統」對話一樣。
-->

---
layout: default
---

# 練習 3 (綜合)：（綜合）：優先順序大挑戰
### 任務說明

下面這個運算式混合了位元運算子、邏輯運算子與比較運算子：

```java
int x = 6, y = 3;
boolean check = x > y & x % y == 0 || x << 1 > 10;
```

請依照「完整運算子優先順序表」，**手動推算**這個運算式的執行順序與最終結果（不要直接執行程式，先在紙上算一次）。

**提示：** 先找出表格中優先順序最高的運算子開始算，再依序往下。

<!--
回顧一下，我們已經看過位元運算子（&、<<）的優先順序比邏輯運算子（&&、||）高，但跟比較運算子、算術運算子之間還有更細的順序關係。這個練習要把整張優先順序表「動手用一次」。

引導思考：這個運算式裡，x % y == 0、x << 1 > 10、x > y 這三個小運算式，誰會先被算出來？再來，算出三個 boolean 值之後，& 跟 || 誰先執行？把這兩個問題分開想，會比一次看整行容易很多。
-->

---

# 練習 3 (綜合)：（綜合）：優先順序大挑戰
### 解題提示

依照優先順序表，由高到低逐步拆解：

```java
int x = 6, y = 3;

// 1. 算術／位移（第 4-6 層）先算：x % y, x << 1
//    x % y = 0, x << 1 = 12

// 2. 比較運算子（第 7-8 層）：x > y, (x % y) == 0, (x << 1) > 10
//    x > y = true, 0 == 0 = true, 12 > 10 = true

// 3. 位元 AND（第 9 層，比 || 高）：true & true = true

// 4. 邏輯 OR（第 13 層）：true || true = true

boolean check = x > y & x % y == 0 || x << 1 > 10;
System.out.println(check); // true
```

<div class="mt-4 p-3 bg-blue-50 border-l-4 border-blue-400 text-gray-700 text-sm text-left">
💡 <b>建議：</b> 實際開發時，遇到這種混合運算子的判斷式，務必加括號明確標示意圖，避免日後維護時的誤讀。
</div>

<!--
這一題把今天學到的兩個重點（位元運算子的優先順序、完整優先順序表）綜合在一起。解題的關鍵分成四個步驟：先算算術跟位移（第 4–6 層），再算比較運算子（第 7–8 層），接著算位元 AND（第 9 層），最後才是邏輯 OR（第 13 層）。

整段算下來，三個比較運算式都是 true，true & true 是 true，true || true 還是 true，所以最終結果是 true。

⚠️ 易錯點：如果照「從左到右」的直覺去看，很容易把 & 跟 || 的順序搞混，或者忘記位移運算子要先算。這也是為什麼前面優先順序表那一頁特別提到：實務上遇到這種寫法，加括號永遠是最安全的做法，能讓自己跟同事都不用每次都翻優先順序表。

完成這兩個練習後，我們對 Java 的運算子體系應該有了更扎實的理解，從日常的加減乘除，到位元層級的操作，再到一張完整的優先順序表，都已經涵蓋到了。
-->

---
layout: end
---
