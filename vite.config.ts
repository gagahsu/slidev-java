import { defineConfig } from 'vite'

export default defineConfig({
  // Slidev configuration via Vite
  slidev: {
    themeConfig: {
      // You can try to override theme fonts here if needed
    },
  },
})
